# server's parameter
httpServer:
    # oracle database linking
    oracle_user: 'wf2prodmoc'
    oracle_password: 'wf2moc_22FAB2'
    oracle_dsn: '10.34.58.15:1521/f2wdb'

    # redis linking
    rds_connection: '10.34.58.42'
    rds_port: 6379
    rds_search_pattern: "Car:monitor:*"

    # controlling_on-off
    # if it is equal to 1, this programme will run constantly until being forced to stop
    # or 0, just run once
    circulating_on: 1

    # slecet-path 1 or 2
    algorithm_on: 1

    # load data
    mapPath: 'data/OHTC_MAP_202404191001.json'
    positionPath: 'data/OHTC_POSITION_202404191001.json'
    order_on: 1 # 1: load local order data, 0: don't load local order data
    order_path: 'data/TRANSFER_TABLE_202404191002.json'
    # reload map data
    build_map: 1
